#Merge sort
from math import floor

# read the array
# split until it split into individual elements
# compare the element for each list and then merge it into another list
#

array = [11,14,23,45,20,32,76,12]

median = int(floor(len(array)) / 2)

def merge_sort(array):

    if len(rt)>1 :
    median = int(floor(len(num)) / 2)
    #dividing array
    rt = array[median::]
    lf = array[:median:]
    #length of array
    len_rt = len(rt)
    len_lf = len(lf)

